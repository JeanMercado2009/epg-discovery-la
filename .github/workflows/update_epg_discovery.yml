name: Actualización Diaria EPG Discovery Kids

on:
  schedule:
    # Se ejecuta todos los días a las 20:30 COT (UTC-5) -> 01:30 UTC
    - cron: '30 1 * * *'
  workflow_dispatch:

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  update-epg:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del repositorio
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: |
          pip install requests pandas openpyxl

      - name: Descargar y Procesar Grilla Discovery
        env:
          TAPKIT_EMAIL: ${{ secrets.TAPKIT_EMAIL }}
          TAPKIT_PASSWORD: ${{ secrets.TAPKIT_PASSWORD }}
        run: |
          python updater.py

      - name: Guardar cambios en el repositorio
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add DKLA_EPG.xml
          if git diff --staged --quiet; then
            echo "No hay cambios nuevos en las grillas de Discovery."
          else
            git commit -m "Auto-update EPG Discovery Kids: $(date +'%Y-%m-%d %H:%M')"
            git push
          fi
